import { useEffect, useState } from "react";
import { useNavigate } from "react-router-dom";
import api from "../services/api";
function Projects(){const navigate=useNavigate();const[projects,setProjects]=useState([]),[search,setSearch]=useState(""),[statusFilter,setStatusFilter]=useState(""),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[error,setError]=useState(""),[showForm,setShowForm]=useState(false),[editingProject,setEditingProject]=useState(null),[name,setName]=useState(""),[description,setDescription]=useState(""),[status,setStatus]=useState("ACTIVE");const loadProjects=async()=>{try{setLoading(true);setError("");const params={};if(search.trim())params.search=search.trim();if(statusFilter)params.status_filter=statusFilter;setProjects((await api.get("/projects",{params})).data)}catch(err){setError(err.response?.data?.detail||"Failed to load projects")}finally{setLoading(false)}};useEffect(()=>{loadProjects()},[search,statusFilter]);const reset=()=>{setName("");setDescription("");setStatus("ACTIVE");setEditingProject(null);setShowForm(false)};const save=async(e)=>{e.preventDefault();try{setSaving(true);setError("");if(editingProject)await api.put(`/projects/${editingProject.id}`,{name,description,status});else await api.post("/projects",{name,description});reset();await loadProjects()}catch(err){setError(err.response?.data?.detail||"Failed to save project")}finally{setSaving(false)}};const edit=p=>{setEditingProject(p);setName(p.name);setDescription(p.description||"");setStatus(p.status);setShowForm(true)};const remove=async id=>{if(!window.confirm("Are you sure you want to delete this project?"))return;try{setSaving(true);await api.delete(`/projects/${id}`);await loadProjects()}catch(err){setError(err.response?.data?.detail||"Failed to delete project")}finally{setSaving(false)}};return <div className="page"><header className="page-header"><div><p className="eyebrow">Delivery workspace</p><h1>Projects</h1><p className="page-subtitle">Organize the initiatives where your team’s best work happens.</p></div><button className="btn btn-primary" onClick={()=>setShowForm(!showForm)}>{showForm?"Close editor":"+ New project"}</button></header>{error&&<p className="notice">{error}</p>}{showForm&&<form className="card panel form-grid" onSubmit={save}><h2>{editingProject?"Edit project":"Create a project"}</h2><input placeholder="Project name" value={name} onChange={e=>setName(e.target.value)} required/><textarea placeholder="Give your team useful context…" value={description} onChange={e=>setDescription(e.target.value)}/>{editingProject&&<select value={status} onChange={e=>setStatus(e.target.value)}><option value="ACTIVE">ACTIVE</option><option value="COMPLETED">COMPLETED</option><option value="ARCHIVED">ARCHIVED</option></select>}<div className="form-actions"><button className="btn btn-primary" disabled={saving}>{saving?"Saving…":editingProject?"Save changes":"Create project"}</button><button type="button" className="btn btn-quiet" onClick={reset}>Cancel</button></div></form>}<section className="projects-toolbar"><input placeholder="Search projects…" value={search} onChange={e=>setSearch(e.target.value)}/><select value={statusFilter} onChange={e=>setStatusFilter(e.target.value)}><option value="">All statuses</option><option value="ACTIVE">Active</option><option value="COMPLETED">Completed</option><option value="ARCHIVED">Archived</option></select></section>{loading?<div className="loading-state">Loading projects…</div>:projects.length===0?<div className="empty-state">No projects match your filters.</div>:<section className="project-grid">{projects.map(p=><article className="card project-card" key={p.id}><div className="project-card-top"><span className={`badge status-${p.status.toLowerCase()}`}>{p.status}</span><span className="muted">#{p.id}</span></div><h2>{p.name}</h2><p>{p.description||"No project description yet."}</p><div className="project-card-footer"><button className="btn btn-primary" onClick={()=>navigate(`/projects/${p.id}`)}>Open project</button><div className="project-card-actions"><button className="icon-btn" onClick={()=>edit(p)} disabled={saving}>Edit</button><button className="icon-btn" onClick={()=>remove(p.id)} disabled={saving}>Delete</button></div></div></article>)}</section>}</div>}
export default Projects;
